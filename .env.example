# Codex Desktop Environment Configuration
# 复制此文件为 .env 并根据需要修改

# =============================================================================
# Development Settings / 开发设置
# =============================================================================

# Node environment (development | production | test)
NODE_ENV=development

# Enable debug logging / 启用调试日志
# DEBUG=codex:*

# =============================================================================
# API Configuration / API 配置
# =============================================================================

# Backend API URL (optional, uses default if not set)
# 后端 API 地址（可选，不设置则使用默认值）
# VITE_API_URL=http://localhost:3000

# WebSocket URL for real-time updates (optional)
# WebSocket 地址用于实时更新（可选）
# VITE_WS_URL=ws://localhost:3000/ws

# =============================================================================
# Feature Flags / 功能开关
# =============================================================================

# Enable experimental features / 启用实验性功能
# VITE_ENABLE_EXPERIMENTAL=false

# Enable verbose logging in production / 在生产环境启用详细日志
# VITE_VERBOSE_LOGGING=false

# =============================================================================
# Tauri / Desktop App Settings
# =============================================================================

# Rust log level for Tauri backend
# RUST_LOG=info

# =============================================================================
# Notes / 说明
# =============================================================================
#
# - Variables prefixed with VITE_ are exposed to the frontend
# - 以 VITE_ 开头的变量会暴露给前端
#
# - Do not commit .env files with sensitive data
# - 不要提交包含敏感数据的 .env 文件
#
# - See README.md for more configuration options
# - 更多配置选项请参阅 README.md
