I will polish the frontend design to improve the visual experience and consistency.

### 1. Sidebar Refinement (`ProjectList.tsx`, `SessionList.tsx`)
- **Visual Hierarchy**: Enhance list items with better spacing and hover states.
- **Active State**: distinct styling for selected items with a subtle background and text color shift.
- **Typography**: Improve readability of project/session names and metadata.

### 2. Chat Input Polish (`ChatInputArea.tsx`)
- **Modern Look**: Add `backdrop-blur` and transparency to the input container for a sleeker feel.
- **Focus Interaction**: Add a visual focus ring to the entire input container when the textarea is active, not just the textarea itself.
- **Transitions**: Smooth out button hover states and interactions.

### 3. Global Styles (`index.css`)
- **Selection**: Customize text selection color to match the theme.
- **Scrollbars**: Ensure scrollbars are subtle and integrated well with the theme.

### 4. General Component Polish
- **Transitions**: Add subtle entry animations for list items and messages where appropriate.
- **Consistency**: Ensure border radius and shadows are consistent across the new changes.
